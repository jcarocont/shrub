% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{fzfind}
\alias{fzfind}
\title{Fuzzy-find files and/or directories by pattern}
\usage{
fzfind(
  pattern,
  from = ".",
  recursive = TRUE,
  ignore.case = TRUE,
  type = c("all", "file", "dir")
)
}
\arguments{
\item{pattern}{Character. Regex pattern to match against paths.}

\item{from}{Character. Root path for the search. Default \code{"."}.}

\item{recursive}{Logical. Whether to search recursively. Default \code{TRUE}.}

\item{ignore.case}{Logical. Case-insensitive matching. Default \code{TRUE}.}

\item{type}{Character. One of \code{"all"}, \code{"file"}, or \code{"dir"}.
Controls whether to search files only, directories only, or both.
Matched with \code{match.arg()}, so partial strings are accepted.}
}
\value{
Character vector of matching paths (full names).
}
\description{
Lists all files, directories, or both under a root path and filters them
using a regex pattern. Combines \code{list.files()} and \code{list.dirs()}
so that the \code{"all"} type truly returns every path, deduplicating
entries that would otherwise appear in both.
}
\examples{
\dontrun{
fzfind("README")                  # find any README file or dir
fzfind("\\\\.csv$", type = "file")  # find CSV files only
fzfind("raw", type = "dir")       # find dirs containing "raw"
}

}
